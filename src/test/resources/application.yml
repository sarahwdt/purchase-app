application:
  precision:
    default: 18

  rounding-mode:
    default: HALF_EVEN

server:
  port: 8090

spring:
  application:
    name: purchase-app

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate